const routes = {
  home: '/',
  category: '/category/:genre',
  product: '/product/:productID',
  contact: '/contact',
  profile: '/profile/:nickname',
  search: '/search',
};

export default routes;
